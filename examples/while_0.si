//command completion을 기다리는 polling loop


init {
  CmdStatus := 1;
  CmdDone := 0;
  WaitCnt := 0;

  handler 0 {
    if CmdStatus = 1
    then CmdDone := 1
    else unit
  };
  handler 1 {
    CmdStatus := 0
  }
}
main {
    while CmdDone = 0 do (
    WaitCnt := WaitCnt + 1
  )
}
