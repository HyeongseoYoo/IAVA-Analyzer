(include_subdirs qualified)

(library
 (name variable_analysis)
 (modules syntax parser lexer analyzer domain itv abs_dom provenance reporter)
 (libraries cohttp-lwt-unix yojson))

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
