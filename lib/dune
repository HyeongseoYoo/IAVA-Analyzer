
(library
 (name variable_analysis)
 (modules syntax parser lexer analyzer interp domain itv abs_dom provenance trace reporter)
 (libraries unix))

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
